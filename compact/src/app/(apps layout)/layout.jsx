'use client';
import MainLayout from '@/layout/app-layout/index.jsx';

const AppsLayout = ({ children }) => {

    return (
        <MainLayout>
            {children}
        </MainLayout>
    )
}

export default AppsLayout
