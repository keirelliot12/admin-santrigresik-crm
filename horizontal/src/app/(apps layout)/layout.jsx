'use client';
import MainLayout from '@/layout/app-layout'

const AppsLayout = ({ children }) => {

    return (
        <MainLayout>
            {children}
        </MainLayout>
    )
}

export default AppsLayout
